---
'@vampaz/vite-plugin-local-tls': patch
---

Keep the shared proxy alive across HTTP/2 client resets, never replace a healthy compatible service during Vite startup, and keep retrying route recovery while Vite remains running.
